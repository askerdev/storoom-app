export enum AllRoles {
  admin = "admin",
  student = "student",
  teacher = "teacher",
  curator = "curator",
}

export enum PaymentType {
  increment = "increment",
  decrement = "decrement",
}

export enum HomeworkStatus {
  checking = "checking",
  approved = "approved",
  returned = "returned",
}
